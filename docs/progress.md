# FieldOps progress

## 2026-09-13 — foundation

- Created independent repository at `C:\Users\User\Documents\Projects\active\fieldops`; no sibling project source used.
- Saved complete requirements in `docs/spec.md` and agreed implementation plan in `docs/implementation-plan.md`.
- Started tracked build/verification goal. Shared domain contracts and pinned application/tooling scaffold written.
- Parallel work: deterministic comparison/demo fixtures; document parsers/free AI adapter; persistence/auth/API/jobs. Root integrates UI, evaluation and documentation.
- Environment: Windows PowerShell, Node 24.19, npm 11.19, working Docker.
- Current external status: no Groq free-tier/ZDR configuration verified; no new Supabase, Trigger or Vercel deployment provisioned. No paid calls or purchases.
- Verification pending; no performance claims made.

## Vertical slice, parser breadth and offline verification

- Six connected screens implemented with browser-local demonstration data and persistent local/API workspace support.
- Genuine PDF, OCR, image, spreadsheet, CSV and pasted-text parsing implemented; strict free Groq adapter remains disabled per explicit user instruction. Manual recovery preserves sources without claiming AI extraction.
- Append-only correction audit, source regions, item grouping, approved-equivalence calculations, packs/MOQ/tiers, currency separation, Excel and print reports implemented.
- Local persistent jobs, private cloud repository, RLS migrations, OAuth allowlist, safe retries/cancellation/deletion and free-quota admission implemented.
- First integrated unit/integration run: 76/76 tests passing. PostgreSQL/RLS tested with PGlite platform stubs; hosted Supabase still unverified.
- Benchmark: 24 self-authored documents, 144 logical items, eight scenarios. Baseline equivalence precision102/102 and recall102/116; these are not AI metrics. See evaluation report for exact denominators and limitations.
- Independent agent review checked a subset of five originals/30rows/200 core expected values and five match judgments. No human verification claimed.
- Observed failures fixed: decimal pack surplus residue; unknown box contents treated as equivalent; demo raw-evidence mismatches; local Next forwarding headers rejected by isolation guard.
- Build and browser/export/accessibility checks underway; no release-completion claim yet.
- Existing Vercel login verified as Hobby. New FieldOps deployment will contain public fictional samples only. No existing project will be reused; private AI/cloud provisioning remains disabled.

## 2026-09-13 — public release verification

- Published the dedicated [FieldOps repository](https://github.com/Chi944/fieldops) and the [public demonstration](https://fieldops-eight-blue.vercel.app). Next.js production build and Vercel deployment passed.
- Integrated checks passed: **94 unit/integration tests across nine files**, **6/6 local browser tests in 24.9 seconds**, and **4/4 public production browser tests in 23.9 seconds**. Browser coverage includes the connected sample workflow, correction/reapproval history, matching edits, downloaded workbook, print rendering, automated accessibility checks and mobile keyboard/dialog use. Local-only tests also exercise real text/PDF/image parsing, preserved originals and manual recovery with AI disabled.
- [GitHub Linux CI](https://github.com/Chi944/fieldops/actions/runs/34711908349) is green at commit `3d97e58`. Results describe the tested revision; subsequent dependency changes need their own validation.
- Verified the hosted `/api/status` reports `canUpload: false`, `canPersist: false`, and `canExtract: false`. Vercel has no project environment variables configured and runs on the verified Hobby plan. The public deployment presents fictional fixtures, with visitor edits stored in that browser.
- The saved offline baseline report retains **24 authored originals**, **102/102 equivalent-pair precision** and **102/116 recall**, measured on gold-normalized rows. It is not AI field extraction or semantic-matching performance. See [evaluation report](evaluation-report.md) for source/parser denominators, timing, hashes and limitations.
- Live AI remains disabled per the user's instruction. Hosted Supabase, OAuth and Trigger are unconfigured; private-cloud and real model acceptance remain unverified. No inference or alternative-provider calls were made.
- Dependency-advisory remediation is in progress. Final audit status and post-update reruns will be recorded after installation completes. Evaluator configuration hashes now include both dependency manifest and lockfile, Node version/platform/architecture and the installed English OCR language-asset SHA-256 (or an explicit missing marker). This prevents processing-environment changes from reusing older live checkpoints or run IDs. New reports include runtime/asset identity; the next baseline rerun remains offline.
- Updated [acceptance status](acceptance-status.md) with verified release evidence, explicit unverified boundaries and deferred features.

## Dependency-update release verification

- Updated Sharp to 0.35.4 and csv-parse to 7.0.2; applied scoped compatible OpenTelemetry, ws and ExcelJS uuid overrides. Runtime audit now reports zero findings. Two development-tool entries remain for one unused Prisma/deepmerge issue; see [dependency review](dependency-review.md). CI now runs the runtime audit.
- Post-update checks pass: TypeScript, ESLint, production build, **95/95 unit/integration tests** across nine files and **6/6 local browser tests** in 43.6 seconds. Added a literal prototype-like CSV header regression. Actual Trigger imports and ExcelJS conditional-formatting round trip also pass without network calls.
- Reran the offline benchmark against final dependencies: `baseline-all-f2a9c86ad905-8dc06a6ab2cb`, 24/24 complete parser manifests, 144/144 authored identifiers, 963/963 source locators and 544/544 bounded regions; baseline pair precision 102/102 and recall 102/116; eight robustness assertions pass. Package text line endings follow the repository LF convention so the committed bytes match the evaluation fingerprint. Historical reports are retained.
- Final CI/deployment refresh follows this source commit. Live AI and hosted private-cloud checks remain unverified; the user asked to leave the integration disabled.

## Final deployment and handoff

- Source revision `165415c` passed [clean Linux CI](https://github.com/Chi944/fieldops/actions/runs/34712631590): dependency installation/audit, OCR setup, TypeScript, lint, 95 tests, production build and all six browser workflows.
- Vercel deployment `dpl_4ccf1VtXgN5qVFaN1osiUGws7haT` is READY at the canonical public URL. All four public browser workflows passed again on this updated deployment in 22.2 seconds. Its published evaluation JSON matches `baseline-all-f2a9c86ad905-8dc06a6ab2cb`; status still disables persistence, uploads and AI.
- Saved the final [release record](release-verification.md), exact external configuration gates, setup/deployment docs, architecture, case study and demonstration script. Local Markdown links resolve. No private data or secrets were published.
- The agreed credential-free fallback is delivered and verified: public labeled demo plus real local parsing/manual review through matching, comparison and both exports. Live model evaluation and hosted private processing remain explicitly deferred until the user configures the documented free services.

## UI refinement and private-pilot assessment

- Refined the workspace with evergreen navigation, a compact resume panel, real next-step links, a review filter and accessible search. Added one generated decorative still life, served as a 36 KB WebP; exact prompt and provenance are in [image assets](image-assets.md). The generator exposes no model selector, so no named image-model version is claimed.
- Improved the source review queue, next-issue navigation, correction evidence, unmatched-offer filtering and source links. The matrix has a keyboard-focusable bounded scroll area and clearer price/issue hierarchy. Print rules remain separate.
- Independent review caught mobile navigation scrolling the focused field out of view, visible next-step labels missing from accessible names, and an unmodified character shortcut. Fixed all three and added browser regression assertions. Final local production build, TypeScript and lint pass; **95/95 unit/integration tests and 7/7 browser tests pass**, with the final browser run taking 27.7 seconds.
- Recorded [the next release](next-release.md): configure and test dedicated hosted Supabase/Auth/Storage/Trigger, separate parser-only cloud uploads from AI readiness, add storage admission/cleanup/operational recovery, validate shared resource limits, and evaluate live AI only after the user configures it. Keep Supabase for this release; Neon Auth and Storage are now beta alternatives but migrating would require reworking and retesting the implemented access boundary.
- Public refresh and clean Linux verification follow this source commit. No model extraction was enabled, provider project provisioned, private document published or existing project modified.

## UI deployment verified

- UI source `486ef98` passed [clean Linux CI](https://github.com/Chi944/fieldops/actions/runs/34714827733), including runtime audit, OCR setup, type/lint checks, 95 unit/integration tests, build and all seven browser workflows.
- Vercel deployment `dpl_EtS4ErNWBDNyJjm53EgjqBVxgZe2` is READY at [FieldOps](https://fieldops-eight-blue.vercel.app). Five public browser tests passed in 25.9 seconds. Captured fresh public sample screenshots for the README and release record.
- Verified the deployed image matches its recorded SHA-256 and public capabilities still disable uploads, private persistence and AI. No credentials or hosted private services were added. The remaining operational work is documented precisely in [next release](next-release.md).

## Personal-use milestone in progress

- Added an explicit parsing-only processing mode on upload intents and durable runs. Hosted upload admission no longer requires a model; user authentication, private storage and the background worker are still required. Legacy records default to parsing only. Terminal source-ready results cannot be promoted to AI by retry or overwritten by late progress/source writes.
- Source review now distinguishes a readable original from a fully reviewed quotation. Manual completion requires a supplier, line items, complete source coverage and an explicit buyer acknowledgment. Edits invalidate that acknowledgment. Fixed an observed stale no-items notice that remained after manual entry.
- Added a personal launcher and readiness check with isolated `.fieldops/personal` data and `.fieldops/personal-next` build output. It binds only to an available loopback port from 3001–3009 and forces model/cloud credentials off in the child process. Added manifest-verified backup and non-overwriting restore tools, with synthetic tests.
- Personal comparisons and sample workspaces now have separate navigation. Browser acceptance, interruption and backup verification are in progress; no new completion claim yet.
- Supabase access is available and the discovered organization is Free, but a new FieldOps project has not been created. The organization choice required by the provisioning tool was requested. This repository has no Supabase, Trigger, OAuth callback or Groq credentials configured. Local use proceeds independently; AI remains disabled.
- Created the new SQL migration through Supabase CLI 2.117.0. Its generated timestamp preceded the existing future-dated migrations, so its version was advanced to follow their prerequisites. Tests execute the actual ordered SQL in embedded PostgreSQL; hosted provider acceptance remains separate.

## Personal milestone verification underway

- All 119 unit/integration tests across 13 files pass, including the six actual migrations in embedded PostgreSQL, parser-only mode isolation, late-result fences, concurrent Windows uploads, manual review, and backup/restore preservation. Browser acceptance and final production build are being completed separately.
- The final offline run `baseline-all-ff504a533a5a-8dc06a6ab2cb` measured 24/24 complete parser manifests, 144/144 authored identifiers, 963/963 source locations, 544/544 bounded regions and 8/8 robustness assertions. Baseline pair precision remains 102/102 and recall 102/116 on gold rows. No model requests were made; AI metrics remain unverified. An earlier run is retained; the final run fingerprints LF-normalized committed evaluator bytes.
- Fixed an actual Windows lock-acquisition failure observed during simultaneous uploads: transient access-denied/busy results now use the existing bounded retry budget, without deleting an unreadable or live lock. Dedicated injected-error tests cover eventual acquisition and bounded failure.

- Final integration passes TypeScript, lint, production build, **120/120 unit/integration tests across 13 files**, runtime dependency audit (zero findings), and **8/8 browser workflows in 24.8 seconds** against the production server. The personal case uses two actual synthetic quotations through source-linked manual review, completion acknowledgment, matching, matrix, Excel round-trip, print/PDF and reload persistence.
- Independent final review found a cloud retry inconsistency: SQL retained an old quota deadline and exhausted wait count on explicit retry. Fixed it to match the local repository, preserving the processing mode. All 16 actual-SQL tests pass, including the new recovery regression. Hosted execution remains unverified.

## Personal workspace launched and public refresh verified

- Source milestone `c1ccf44` is pushed to the dedicated repository. Vercel deployment `dpl_HJ6hcdhd78FskbKguCyhZLnvrbkC` is READY at the canonical public URL. All **5/5 public browser checks pass in 22.8 seconds**. Hosted status still reports demo mode, parser-only, and no persistence/uploads/extraction; there are no Vercel environment variables.
- Started `npm run personal` successfully on this computer at `http://127.0.0.1:3001`. The actual listener binds only to 127.0.0.1. API status confirms local persistence/uploads, parser-only mode and disabled extraction. The initial personal comparison count is zero; no test fixtures or sample quotations were copied into private data. Visually checked the personal onboarding page and opened the app panel. Port 3000 was not used or stopped.
- Personal source/state stays under ignored `.fieldops/personal`; only repository code, synthetic evaluation results and documentation were published. Restored Next-generated local type/config housekeeping to the committed production defaults after startup and rechecked the live local status. The personal session remains running for the user.

- [Clean Linux CI](https://github.com/Chi944/fieldops/actions/runs/34716991201) completed successfully at `c1ccf44`, including all eight browser workflows in 1.0 minute. Personal local use is verified and available. Supabase organization selection, a dedicated project, OAuth configuration and Trigger credentials remain required before hosted private acceptance; no new provider project or billing was enabled. Live AI stays disabled under the user instruction.

## Current milestone — Neon migration, Trigger development and visual refinement

- Followed the user's Neon-only direction: the active cloud implementation now uses Neon PostgreSQL, managed Auth and private S3-compatible storage. Created only the dedicated FieldOps Free project, applied its migration in one transaction, provisioned a restricted runtime login and inserted the confirmed numeric GitHub invitation. Live database checks deny direct comparison insertion and managed session-token reads. Historical backend files remain as regression history, not active deployment instructions.
- Completed the generated-reference UI refinement and subsequent independent review fixes: visible mobile workflow orientation, issue-first source selection, explicit issue labels and a more compact mobile matrix. Saved six fresh desktop/mobile captures from the local production build, with only fictional samples. README now points to these current captures and labels their provenance.
- Latest local verification: **172 unit/integration tests pass**, **9/9 browser workflows pass in 27.0 seconds**, TypeScript/ESLint/production build pass, and the runtime dependency audit reports **zero findings**. These results do not assert final clean Linux CI or a refreshed public deployment; both remain pending for the current source milestone.
- Connected the dedicated Trigger Free project `proj_gqdrztfnxotydnuhiaku` using the default CLI profile and a named Development-only key stored in ignored local configuration. Pinned SDK/build/CLI to 4.5.16, registered all three tasks on `node-24`, and completed synthetic run `run_06g9f02hnt6o8ef655fuqn0r01`: 18 preserved source cells, USD `251.30`, and zero database/model/private-file calls. Added reproducible [development instructions](trigger-development.md). No production worker was deployed by this check.
- Groq browser access now works in the Personal/Default Project view; Free Plan, ZDR and API-key configuration have not been verified. Neon browser access currently opens the `des` Free organization, which does not contain the dedicated FieldOps resource. The exact resource's Vercel integration is being used to resolve access; no unrelated organization/project is substituted. Older blanket sign-in blockers are superseded by these narrower target-configuration gates.
- Hosted GitHub authentication, private object storage/CORS, production Trigger deployment and live AI acceptance remain incomplete. No supplier document was published, no model request was made and no paid service was purchased. Local personal parsing/manual comparison continues independently.

## Direct-account cloud setup and live reliability work

- Connected Vercel project `prj_E3g3MNRejByI5oe4BHBKgViFusaN` to `Chi944/fieldops`; API verification confirms GitHub, production `main`, and Git deployments enabled. Future main pushes now deploy automatically. Publication of these working-tree changes is still pending.
- Created `jolly-queen-28409793` in the requested directly authenticated Neon account, Free plan, with main branch `br-sweet-sun-ayzs00zw`. This supersedes the blocked marketplace activation flow. Applied the exact migration, restricted roles and intended invitation; 16/16 tables have RLS and actual prohibited runtime operations were denied. No existing comparison data was migrated.
- Configured managed Auth and private Neon storage. A real synthetic storage check passed exact-origin CORS, signed staging upload, hash/size finalization, signed download, unsigned denial and cleanup. New restricted runtime/storage/Auth settings are sensitive Vercel Production variables; the worker received only required runtime/storage settings. Hosted OAuth and worker smoke acceptance are in progress.
- Created the dedicated GitHub OAuth app. After user account confirmation, the first unused OAuth secret appeared in a diagnostic; immediately generated a replacement, saved it privately, deleted the first secret, and verified only the replacement remains. No active credential is committed.
- Verified Groq Free and inference ZDR, created a dedicated FieldOps key, and ran actual tiny synthetic extractions. A separate local AI workspace on port3003 keeps personal port3001 data untouched. Multi-item development probes revealed provider schema failures, which are retained privately and counted; held-out data remains unrun. Invalid responses no longer poison accepted checkpoints, and original/corrected values survive retry.
- Added explicit personal `--ai` activation with validation and per-port build isolation; default personal mode stays parser-only. Added rejected-response diagnostics without document text in operational logs. Fixed abandoned pasted-source ownership and bounded cleanup so maintenance cannot starve queued processing.
- Latest checks at this point: **201/201 unit/integration tests**, TypeScript/lint/build pass, **9/9 production browser workflows in30.8s**. CI now starts the production bundle after building it. Final source checks and clean hosted publication follow the remaining configuration changes.
- All services remain Free/Hobby, with no payment details or paid fallback added. Trigger uses the included-plan limit and active-run cancellation; reservations retain maintenance headroom. Neon storage is currently free during beta, so permanent free storage is not claimed.

- Published source `57101e4` after **204/204 tests**, type/lint/build and nine production browser workflows passed. The Git push automatically created Vercel production deployment `dpl_AXptvX5Bvs9N9F5hRivTeUKnEyK7`, READY at the canonical URL. Clean Linux CI run34722275577 is queued.
- Trigger production deployment `ewdnylwz`/`20260912.1` succeeded on Linux/node24. Synthetic production health run `run_06g9fccf6cj1rqdajck7oosu01` completed with18sources and USD251.30. Hosted application remains parser-only; no production Groq key installed.
- Actual GitHub OAuth created a valid invited Neon session, but the provider returned its verifier to the homepage outside the original proxy matcher. This observed hosted integration failure is being fixed and regression-tested before private-workspace acceptance. No account bypass or manual session-token injection is used.

## Hosted personal use and free-capacity hardening verified

- Published `6dab810`; its automatic Vercel deployment is READY and [clean Linux CI](https://github.com/Chi944/fieldops/actions/runs/34722616140) passed. The homepage-verifier fix passed real invited GitHub sign-in, sign-out and restored access after a fresh sign-in.
- Completed two self-authored pasted originals through private Neon storage and actual production Trigger jobs, source-linked manual corrections, explicit coverage review, approved baseline matching, USD 25.00 versus 22.50 comparison and a 13-sheet revision-12 Excel workbook. Unknown delivery blocked an overall winner. Reload preserved the report. Anonymous originals returned 401. UI deletion removed the comparison and both canonical objects returned 404; outbox retirement follows the upload-replay grace period. [Hosted evidence](hosted-acceptance.md) records exact runs and limitations.
- Completed a separate local AI workflow with two tiny synthetic quotations and an actual source-linked AI match. A corrected price survived retry, preserved its original value and kept the resulting discrepancy visible through matrix and revision-7 Excel. Larger development PDF extraction still failed; accepted chunks survived the failed retry and no partial quotation was published. Held-out model requests remain zero. Hosted AI stays disabled; no paid model fallback was enabled.
- Audited the latest zero-spend instruction. All configured providers remain Free/Hobby. Fixed continuous polling that could keep Neon awake: terminal comparisons and hidden tabs now stop, focus/actions refresh, quota deadlines delay polling, and an observation fence prevents an older response from stopping newly queued work. Eight timer regressions cover those behaviors. Neon storage is currently free only during beta; no permanent free-storage promise is made.
- Final local checks: **230/230 unit/integration tests across 27 files**, TypeScript, lint, production build, **9/9 browser workflows in 27.5 seconds**, and zero runtime dependency audit findings. The browser suite uses isolated synthetic data and includes Excel round-trip, print/PDF, persistence, PDF/image evidence, keyboard and automated accessibility checks. Personal port 3001 and its data remain untouched. Final Git publication and fresh public checks follow this record.
- Published `5aa72fe` through the connected Git repository. Automatic Vercel deployment `dpl_9NLcmNWDZPB8YZBn9Nxbckxkqjv8` is READY; all six applicable public browser checks passed in 31.4 seconds, with three local-only cases skipped. The public offline report now serves the final 24-document run `baseline-all-c46b4849dea0-8dc06a6ab2cb`. A follow-up puts the small AI successes and failed complete-document probe directly on the reliability page, with a link to their separate evidence.
- [Clean Linux CI for `5aa72fe`](https://github.com/Chi944/fieldops/actions/runs/34723941700) passed installation, runtime audit, OCR preparation, type/lint checks, all 230 tests, production build and all nine browser workflows. Only authored synthetic data and measured reports were published; no credentials or private originals entered Git.

## Production hardening — 20 September 2026

- Scope: improve the existing private pilot without adding paid services, replacing its Neon project, or claiming unmeasured AI reliability. Preserve the working parser/manual workflow and independent public demo.
- Rechecked Groq: the FieldOps project is selected in the Personal organization, Free $0 is the current plan, and Inference APIs Zero Data Retention is enabled. Live development evaluation has exclusive, bounded access to the existing free key. Hosted AI remains disabled.
- Replaced unbounded JSON body buffering with a 2 MiB byte-limited streamed reader, a 15-second deadline, malformed UTF-8/length rejection and cancellation. Actual API routes now exercise fresh invitation/session checks and two-owner isolation against the shipped PostgreSQL migrations.
- Added an on-demand private workspace status panel for reserved original-file capacity, pending cleanup, processing availability and job counts. Other users' usage is never returned; the panel does not continuously poll or pretend configuration is a service health test.
- Applied the incremental migration for atomic workspace/project capacity, 24-hour abandoned-upload expiry and deletion reservations. Regression work caught skipped same-comparison expiry candidates and non-cascading managed-user foreign keys; both were fixed before hosted application. Six real concurrent PostgreSQL tests pass.
- Five hosted synthetic formats completed with 197 source spans; their five originals/538,486 bytes restored to a separate local workspace with verified hashes. Added an operator cloud-upload pause flag that preserves saved work and personal local use.
- Final local checks: 296/296 tests across 34 files, typecheck, lint, production build, zero runtime audit findings and 9/9 browser workflows in 27.3 seconds. The refreshed 24-document offline baseline and separate failed AI probes are published as distinct reports; no held-out AI requests occurred.
- Deployed Trigger `20260920.1` (`6vls4g3j`). Fresh worker task checks, Git-linked web publication, clean Linux CI and final hosted acceptance remain in progress. The new production-readiness record explicitly retains the AI, database-history-growth and two-real-hosted-identities gates.
- Published hardening commit `a220746`: clean Linux CI passed and automatic Vercel deployment `dpl_FGABQT66szMheMCfoDsJDkhZ2SF8` is READY. Its live status panel correctly reported owned capacity and one failed source. New worker JPEG/CSV jobs passed while a malformed PDF failed with recovery guidance; all anonymous original requests returned 401.
- Live duplicate testing exposed discarded API error codes and an ineffective Retry action. Added structured error recovery, existing-original access and explicit separate-copy behavior. Final checks after this fix: 299/299 unit/integration tests and 10/10 browser workflows in 34.2 seconds, with type/lint/build passing. Publishing that verified fix next; no processing/model configuration changed.
- Published `6999678`; clean Linux CI passed and Git-triggered Vercel deployment `dpl_8JwpzMGMy7CjQZYBpuDcGD1qzYyH` is READY. All six applicable public browser checks passed in 34.1 seconds, with four local-only cases skipped. Actual signed-in duplicate recovery then created one separate source-ready CSV while keeping its original intact. Only the two disposable synthetic comparisons are queued for scoped cleanup; provider reservations remain intact.
- Scoped acceptance cleanup completed at 12:09:08 UTC after the real six-minute grace. Both synthetic comparisons and nine originals are removed; all 18 canonical/staging object checks return 404, no tombstones remain, and reserved original capacity is zero. Processing reservations stay 11 / USD 1.43. The separate recovery backup and personal data remain untouched. The updated scheduled worker also completed its 11:45/12:00 runs. Final documentation records these results and the still-open broad AI/security/capacity release gates.

## Complete-quotation development evaluation — 21 September 2026 UTC

- Fixed the cohort before new model responses: three complete synthetic development originals (two-page industrial PDF, translation text and office CSV), 18 logical items and 129 critical fields. Added a guarded, resumable `eval:dev` runner with an isolated development manifest, exact source/code/runtime hashes, immutable reports, durable request reservations and explicit unknown usage. No held-out model requests or production AI calls were made.
- The original baseline and the first two candidates each returned zero usable quotations. Preserved all three formal results plus an excluded interrupted preflight. Diagnosed actual context/target coverage conflicts, an item/MOQ chunk split, malformed provider JSON, unsupported literal excerpts and unit-bearing values in decimal-only fields. The report distinguishes these failures and does not infer token exhaustion.
- The final explicit `retain_valid_chunks_v1` development policy preserved only validated sections, marked every failed target source unresolved and retained the existing application fail-fast default. On the same full originals and budgets, it returned **0/3 complete, 2 partial, 1 rejected**: **42/129 critical fields**, **48/138 stated fields**, **6/18 items**. This improves available reviewed data; complete-document extraction remains below release requirements. No supplier-value, excerpt or citation repair was introduced.
- A separate audit verified identical original source arrays, exact failed target sets (34 records), 68/68 resolvable field references and all six retained prices blocked by coverage review. An independent label-based numeric gold audit matched 54 assertions against the originals; it is not human verification or a complete semantic audit.
- Final phase: 11 returned responses, 32,727 known input/output tokens, two responses without usage, 636.3 seconds including 599.8 seconds of quota pacing. Entire investigation: 28 responses, 95,620 known tokens, three responses without usage. Four formal phases reuse three distinct originals. Provider invoice cost was not measured; no purchases, paid fallback or billing changes occurred.
- Final verification passed **354/354 tests across 44 files**, including six real PostgreSQL concurrency tests, TypeScript, ESLint, production build, zero runtime dependency audit findings and **10/10 isolated browser workflows in 50.6 seconds**. Personal workspace data and the existing server on port 3000 were untouched.
- At 15:55:10 UTC, the read-only production check still returned `processingMode=parse_only`, `canExtract=false`; local production configuration hashes and remote `main` at `660382b` were unchanged. Work is isolated on `fieldops/development-extraction-evaluation` for review, with no production deployment. The [measured study](ai-development-study-2026-09-21.md) and [reproduction protocol](development-evaluation-protocol.md) publish results, failures and limitations without private response bodies or credentials.
- Published code/results as `5bcc5de` in [draft PR #1](https://github.com/Chi944/fieldops/pull/1). All 22 fingerprinted files matched staged Git blobs, and a final 57-file publication audit found no credentials, private responses or held-out originals. GitHub's private-email protection initially blocked the unpublished commit; it was amended to the account's no-reply address before the successful push, without changing account settings. Production `main` remains unchanged. [Linux verification](https://github.com/Chi944/fieldops/actions/runs/35623261333) runs independently of the passing local checks.
- Clean Linux verification completed successfully for `5bcc5de`: installation, runtime audit, OCR preparation, TypeScript, lint, unit/integration tests, production build and browser workflows all passed. Subsequent commits update documentation only. Final remote check still places production `main` at `660382b`; the draft remains unmerged and production AI remains disabled.

## Complete-document reliability follow-up — 22 September 2026

- Added source-based completeness checks so identifiers alone do not hide omitted quantities/prices. Reproduced and fixed a false positive for a source-supported CSV total footer; priced items named “Total” remain checked. Evidence excerpts, parser spans, decimals and original interpretations are preserved.
- Reproduced two distinct provider request-schema failures with typed output. Nullable primitive serialization preserved all constraints, then a partitioned contract removed competing object discriminators. Both remain explicit development options; the application default remains legacy. New sanitized request diagnostics avoid additional diagnostic calls and exclude raw messages/document contents.
- Measured the same three complete development originals with original baseline hashes and fixed limits. The final candidate returned **0/3 complete, 2 partial, 1 rejected; 22/129 critical fields, 24/138 stated fields and 3/18 items**. This regresses from the previous recovery result (42/129, 48/138, 6/18), so it is not promoted. No held-out model calls or production AI enablement occurred.
- Added and ran an offline rejection audit: all eight rejected generations were valid JSON with invalid structure, primarily item/charge property confusion, missing item fields and context exclusions. Original arrays are identical in both partial outputs; 39/39 field references resolve. All three retained prices remain blocked, with no cost recommendation.
- Final phase used 11 dispatches, 22 reserved attempt slots and 158,442 estimated tokens. Known usage is 13,735 tokens; 8 responses lack usage. Across this follow-up, 17 dispatches reserved 34 slots/243,356 estimated tokens; 14 dispatches have unknown usage. No paid fallback, purchase or billing change was made; invoice cost is unavailable. Full results and failures are in the [study](ai-development-study-2026-09-22.md).
- Captured the measured implementation as `ae4a65a`, after checking all 28 fingerprinted staged blobs and 52 publication files. Preserved the observed positional charge-metric defect in the historical score. A separate regression then caught equal tax/shipping amounts incorrectly passing the selected-annotation gate; readiness v3 now checks charge kind for stated fields too. The live report remains gate v2, with no post-hoc rewrite or new model call.
- Final local verification: **429/429 tests across 54 files** in 29.17 seconds, including real PostgreSQL concurrency; typecheck/lint, production build, zero runtime dependency audit findings and **10/10 browser workflows** in 34.5 seconds. The browser suite tests the actual manual and demo workflows with AI disabled. The post-run scorer fix changed only evaluation code/tests; the measured application bundle is unchanged.
- Read-only production status still reports `parse_only` and `canExtract=false`; local production configuration and `main` at `660382b` are unchanged. The existing port 3000 listener and personal data were not touched. Changes remain on the draft review branch; final publication and Linux CI are recorded below when complete.
- Published the measured snapshot and the separately tested scorer guard as `ae4a65a` and `e761f51` in [draft PR #1](https://github.com/Chi944/fieldops/pull/1). [Clean Linux verification of `e761f51`](https://github.com/Chi944/fieldops/actions/runs/35630594687) passed installation, runtime audit, OCR setup, TypeScript, lint, all tests, production build and browser workflows in 2m45s. This subsequent documentation update records those results only; no runtime change, merge or production deployment is included.

## Release integration — 23 September 2026

- The user authorized committing, pushing and merging the finished changes. Independent review found no isolation or deterministic-comparison regression in the existing PR. Hosted AI remains disabled, production configuration is unchanged, and the active personal server/data are outside the test workspace.
- Froze a new homogeneous fact-ledger development transport after source-preservation, incomplete-tier/discount, ambiguous-charge and misplaced-field regression checks. It uses the same model and limits; offline traversal accounts for all three complete originals in 11 requests, 22 reserved attempt slots and 118,920 estimated reserved tokens. A preliminary offline capture made no provider calls; the final capture pins the frozen code. No held-out inputs inform this work.
- The frozen implementation passes **463/463 tests across 56 files**, including real PostgreSQL concurrency, TypeScript, ESLint, production build, zero runtime dependency vulnerabilities and **10/10 browser workflows in 49.2 seconds**. These software checks are separate from the ongoing full-document AI measurement.
- The first response exposed an application root-entity convention failure despite valid JSON/schema and a normal stop. The original live phase is unchanged. A separate, zero-model-call decoder replay is planned after preserving its snapshot; [the release study](release-study-2026-09-23.md) records its narrow scope and the stricter charge-gate limitation in advance.
- Preserved the original live fact experiment at `92e786f` and the root-only replay at `36d405e`. A second separately labelled offline replay fixes a source-supported tier interval falsely read as a negative upper bound. Independent review caught and fixed a comma-number ambiguity before the replay. Both corrections preserve raw values, parser references and generic monetary validation; no model response or historical report was repaired.
- Latest measured replay: **0/3 complete, 3 partial; 47/129 critical fields, 55/138 stated fields and 7/18 identifier-aligned expected items**, versus 6/129, 6/138 and 1/18 in the original live run. All 117 field references resolve, all original source arrays remain intact, and all 10 retained prices stay blocked. Four provider-schema failures and one local decoder rejection remain. The two replays made zero model calls; held-out data stayed untouched. These are adaptive development measurements, not independent validation or reliable automatic extraction.
- Final local software verification after both fixes: **485/485 tests across 58 files** in 33.37 seconds, including six real Docker PostgreSQL concurrency/isolation tests; TypeScript, ESLint and production build pass; runtime dependency audit reports zero vulnerabilities; **10/10 isolated production-browser workflows pass in 46.4 seconds**. Tests cover upload, evidence review, corrections, matching, comparison, Excel/print, persistence, duplicates, original PDF/image views, keyboard operation and automated accessibility. Personal port 3000 remains PID 30516 with its data untouched.
- PR #1 was merged by the repository cleanup task during this work, at `183e9d3`, with passing Linux checks and no unresolved review threads. New measured snapshots and corrections will be published in a separate follow-up PR; production AI remains disabled. The parser/manual workflow is the supported pilot. Full AI extraction, two-real-hosted-identity validation, derived-history capacity and broader distributed AI admission remain explicit release gates in [production readiness](production-readiness.md).
- Published final correction and tier-range replay as `d5534a2`; all 30 recorded source hashes and the replay helper hash matched staged Git blobs, with no credentials/private files found. [PR #2](https://github.com/Chi944/fieldops/pull/2) passed independent review, both Linux CI runs and its Vercel preview, then merged as `51c7a02`. Both repository PRs are merged with no unresolved review threads.
- Git automatically deployed `51c7a02` as READY `dpl_6LaRVqpxBJPji9NAqN6kotztinsM`. All **6/6 applicable public browser checks passed in 34.3 seconds**; four private/local-only tests passed separately in the local suite. Public status remains cloud/parser-only with extraction disabled; anonymous comparisons return 401/no-store. Private production configuration hashes remain unchanged, and the public reliability page serves the latest measurements. [Release verification](release-verification.md) records exact CI/deployment identities and limits. A documentation-only follow-up records this completed acceptance without changing the measured implementation.
- [Main-branch Linux verification](https://github.com/Chi944/fieldops/actions/runs/35789654293) passed after merge in 2m57s, including the complete software/build/browser workflow. No open PR or unresolved review remains. Automatic full-document AI reliability remains an explicit unfulfilled release gate; the tested parser/manual pilot is the supported deployment.

## Groq focused development study — 23 September 2026

- The user selected Groq first. Reused the existing dedicated development key and Free/ZDR configuration; authenticated read-only model discovery returned GPT-OSS120B and Qwen3.8-27B active. No production AI key, billing change or paid fallback was introduced. The Qwen preview remains an explicit local study option; GPT-OSS and the legacy extraction path remain application defaults.
- Reproduced a specific failure in the previous saved development replies: all four provider-schema rejections contained known context IDs in a target-only exclusion list. Added caller-assigned item slots and separate item/document tasks, deriving coverage from actual evidence instead of model exclusion bookkeeping. Exact repeated PDF preambles and recognized spreadsheet commercial-summary rows retain every original source. Missing facts, wrong scopes, unsupported charge metadata and uncited continuations remain review blockers.
- Froze the implementation at `c51c3b6`. The [preflight](../eval/results/development/groq-focused-2026-09-23/offline-preflight.json) independently captures both model profiles: all 99 source records, three complete development originals, 18 logical items, 12 requests and 58,019 estimated reserved tokens per model. Both fingerprints match all 34 frozen Git blobs. No held-out material informed these changes; model evaluation reads only the separate development manifest. Existing offline regression tests still verify the integrity of the full benchmark's stored hashes.
- Final implementation verification: **535/535 tests across 62 files**, including real PostgreSQL integration, TypeScript/build, ESLint and **10/10 isolated browser workflows in 42.8 seconds**; runtime dependency audit reports zero vulnerabilities. Independent review verified source ownership, evidence guards, model/profile isolation, full-denominator scoring and durable shared quota admission.
- Started the frozen GPT-OSS phase under one-attempt requests and a persistent 180,000 estimated-token UTC-day ceiling. Earlier same-day development reservations total 118,920, so only one new 60,000 phase fits before reset. The second model must use the same frozen code and wait for complete-phase admission; no budget reset or missing-document denominator reduction is allowed. The [study protocol and exact commands](groq-focused-study-2026-09-23.md) preserve the pending comparison. Live measurements and the post-run audit will be recorded after completion.
- The runner stopped after seven responses; its process was confirmed absent, with cause unverified. Closed the study as interrupted and published an immutable report instead of retrying rejected responses. Only industrial returned a quotation: six items and 40/43 selected critical fields, marked partial. Translation had no final output and office was unattempted, giving **40/129 critical fields and 6/18 items** across the original full cohort. Known usage is 36,309 tokens; no provider invoice cost or complete elapsed time is claimed. Original responses, journals, reservations and source arrays remain preserved.
- Diagnosed the industrial document rejection as a date contract mismatch: a literal full-month date failed the ISO-only domain validator, and the focused provider contract omitted that requirement. Added a narrow source-backed written-date canonicalizer and explicit focused date schema/prompt. Invalid dates, numeric ambiguity and unsupported excerpts still fail; no source wording, coordinates, model responses or supplier amounts are repaired.
- Independent review strengthened the zero-call replay to bind the exact original source and gold hashes. Recovery review also reproduced a quota-accounting defect where an unknown request's reservation could be offset by another request's overrun; the new protocol requires per-request no-refund accounting with the full-phase floor. These are development harness changes, not production AI enablement. The [follow-up protocol](groq-focused-study-v2-2026-09-23.md) records the fresh-run boundary and remaining validation.
- Measured the saved-response replay on the one fully observed industrial original: **40/43 → 43/43 critical fields; 40/46 → 45/46 stated fields; 6/6 items unchanged**. All four tasks now validate, but three completeness/evidence issues keep the result partial and all six cost rows blocked. Every original source record is unchanged and 73/73 field references resolve. This zero-call adaptive replay neither reconstructs the unfinished documents nor verifies the new provider date contract.
- Froze protocol 2 with durable exact-request outcomes, immutable completed-output identities, original phase/session timing and conservative per-request accounting. The zero-call preflight captures **12 requests, 58,648 estimated tokens and all 99 source records per model**; both live phases remain unrun because the current shared day has only 1,080 tokens available. Archived only the verified stopped shared lock, preserving its bytes, the original phase lock and the quota ledger.
- Final local verification passes **577/577 tests across 67 files in 26.88 seconds**, including real PostgreSQL integration; TypeScript, ESLint and production build pass; **10/10 browser workflows pass in 34.7 seconds**. The prior runtime audit has zero vulnerabilities and dependencies did not change. All 11 private environment hashes remain unchanged. Production remains parser-only with AI disabled. Publication and clean Linux checks follow below.
- Published the frozen correction and measured reports as `6aecd42`. All 36 implementation files for both model fingerprints and the three replay-helper hashes matched staged blobs; a 35-file publication audit found no credentials, private responses or held-out originals. [PR #3](https://github.com/Chi944/fieldops/pull/3) passed both clean Linux checks ([branch](https://github.com/Chi944/fieldops/actions/runs/35795424710), [PR](https://github.com/Chi944/fieldops/actions/runs/35795428980)) and its Vercel preview, then merged as `b11ce8d`. No open PR or unresolved review thread remains. The fresh v2 live comparison is still pending; merge does not enable production AI.
- Git deployed `b11ce8d` successfully (GitHub production deployment `6602591741`). The canonical app passes **6/6 public browser checks in 29.4 seconds**; four private/local-only cases remain covered by the passing isolated suite. The read-only 23:08 UTC check confirms cloud/parser-only, extraction disabled, and anonymous comparisons denied with 401/no-store. [Release verification](release-verification.md) records the deployed identity and limits. No private configuration or worker deployment changed.

## Conditional production activation run — 23 September 2026

- Executed the user's requested frozen Groq v2 comparison with existing Free/ZDR development credentials, after UTC-day admission allowed both full 90,000-token reservations. Authenticated model discovery confirmed both models active. The 36-file implementation, source originals, annotations, prompts, model profiles and limits remained unchanged; no held-out original or model request was used.
- GPT-OSS finished all 12 requests: **0/3 complete, 3 partial; 77/129 critical fields, 83/138 stated fields, 10/18 identifier-aligned items**. Four tasks failed: two provider array-shape rejections, a duplicate core key and a unit-bearing numeric value. All 99 source records remain intact; 134/134 field references resolve and all ten retained prices remain blocked.
- Qwen stopped during office with an operational quota error after ten dispatches/nine responses: **0/3 complete, 2 partial, 1 quota-aborted; 54/129 critical fields, 59/138 stated fields, 8/18 aligned items**. Two interpretation failures precede the quota stop. Both returned source arrays (72 records) are unchanged, 131/131 references resolve and all eight prices remain blocked. No final office quotation is substituted from its single saved task. Failed documents stay in all full-cohort recall denominators.
- Published fresh immutable phase reports, comparison and read-only focused audits. Independent review reproduced scores, source hashes, task coverage and the deterministic cost guard; no inference repair or retry was made. Disclosed valid alternate CSV citations omitted from frozen annotations, positional missing-charge scoring, and output-dependent source denominators. The quota-limited pair cannot rank intrinsic model quality, cost or speed. [Measured report and exact limitations](groq-live-validation-2026-09-23.md).
- Known usage is 51,173 tokens for GPT-OSS (two responses without usage) and 13,531 for Qwen (one response without usage plus one operational dispatch without a response). Both complete-phase reservations remain charged, exhausting the 180,000 local daily allowance; this is not a provider invoice or account-quota measurement. No purchase, billing change, paid fallback or new credentials were introduced.
- **Production activation condition failed.** Production remains `cloud`/`parse_only`/`canExtract=false`; eleven private environment hashes were unchanged at 05:01 UTC. This publication changes only results/docs, with no worker or runtime deployment change. The already verified 577-test/10-browser implementation remains frozen; those suites are not represented as rerun during inference. Fresh audit/comparison checks pass, and the parser/manual workflow remains the supported pilot.

## Typed item contract and billing review — 23 September 2026

- Implemented the user's next fix as explicit `focused_fields_v2`/`quotation-v10`, retaining v1 behavior and application defaults. All 16 core item field objects are required; numeric optional fields have canonical decimal constraints and the repeated optional-field list is removed. The adapter only changes shape, preserving scalar values, raw excerpts, parser references, state distinctions and incomplete-source handling.
- Added source-linked billing review issues for non-goods items that the existing billing vocabulary cannot compare. The deterministic price/matching veto already existed; the missing step was explaining it during review. Audited corrections remain effective and stale affected approvals.
- Independent review reproduced unit-only evidence being accepted as hourly billing. The v2 path now retains the assertion but raises `unverified_evidence` for unit-only, description-only, wrong-family, alternative or tested negated wording. Added explicit negative-form and contraction regressions. A full-path test isolates this guard: the same retained 150.00 amount becomes needs-review and yields no cost recommendation. No monetary calculation or equivalence rule was relaxed.
- Published an immutable zero-model-call [contract audit and preflight](groq-typed-contract-2026-09-23.md). All six original rejections remain rejected; fifteen expanded checkpoints cannot establish new wire compliance. Three unchanged invalid numeric fragments now fail the actual field schema earlier. No original response, missing billing fact, historical score or held-out input was changed. Planning remains three complete development originals, 18 items, 99 source records and 12 calls; estimated tokens rise 58,648→63,339, within unchanged caps.
- Fresh final verification: **600/600 tests in 71 files, 38.19 seconds**, including real Docker PostgreSQL; TypeScript, ESLint and production build pass; **10/10 isolated browser workflows pass in 34.1 seconds**. The local daily model allowance remains 0 after the previous pair; no live inference, quota refund, production enablement, paid fallback or provider configuration change occurred. All eleven private environment hashes remain unchanged. At 05:30 UTC the canonical deployment still reports cloud/parser-only with extraction disabled.
- Published and merged [PR #5](https://github.com/Chi944/fieldops/pull/5) at `f8dc35c` (05:38 UTC), after independent review and both [pull-request CI](https://github.com/Chi944/fieldops/actions/runs/35822968305) and [branch CI](https://github.com/Chi944/fieldops/actions/runs/35822955292) passed. Clean Linux verification includes PostgreSQL tests, build, zero runtime dependency audit findings and all ten browser workflows (46.2 seconds on the PR run). All 37 implementation fingerprints match the immutable preflight, 120 local documentation links resolve, and publication scans found no credentials or private response files. No open PRs or unresolved review threads remained after merge. This documentation-only follow-up records release evidence without changing the frozen candidate.
